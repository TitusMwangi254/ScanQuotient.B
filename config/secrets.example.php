<?php
/**
 * Copy this file to `config/secrets.php` (which is gitignored) and fill in real values.
 * This keeps secrets out of git history and avoids GitHub push protection blocks.
 */
return [
    // Prefer environment variables when available:
    // setx OPENAI_API_KEY "sk-..."  (Windows) then restart Apache/PHP
    // or set it in your web server env
    'OPENAI_API_KEY' => '',
];

